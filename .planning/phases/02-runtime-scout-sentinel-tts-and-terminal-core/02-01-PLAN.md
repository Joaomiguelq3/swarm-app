---
phase: "02"
plan: "01"
title: "Dependencies and runtime config"
type: "implementation"
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "package-lock.json"
  - "src/runtimes.js"
  - "scripts/check-runtimes.js"
autonomous: true
requirements_addressed:
  - "RUN-01"
  - "RUN-02"
  - "RUN-06"
  - "RUN-07"
context_decisions:
  - "D-01"
  - "D-04"
  - "D-05"
  - "D-06"
must_haves:
  truths:
    - "node-pty is installed/configured for later PTY smoke tests."
    - "src/runtimes.js exports Claude Code and Codex runtime definitions."
    - "Claude Code config uses claude --dangerously-skip-permissions, CLAUDE.md, and .claude/skills/."
    - "Codex config uses codex --approval-mode full-auto, AGENTS.md, and .codex/skills/."
    - "No real Claude/Codex agent process is spawned in this phase."
---

# Plan 02-01: Dependencies and Runtime Config

<objective>
Add the Phase 2 dependencies and create the runtime definition contract used by later workspace and swarm orchestration phases.
</objective>

<context>
Read before executing:

- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/phases/02-runtime-scout-sentinel-tts-and-terminal-core/02-CONTEXT.md`
- `.planning/phases/02-runtime-scout-sentinel-tts-and-terminal-core/02-RESEARCH.md`
- `AGENTS.md`
</context>

<tasks>
<task id="T1" type="auto">
  <title>Add Phase 2 dependencies and scripts</title>
  <files>
    <file>package.json</file>
    <file>package-lock.json</file>
  </files>
  <action>
    Install `node-pty` and `chokidar` with `npm.cmd install node-pty chokidar`. Extend `package.json` scripts without removing existing scripts:
    - keep `start`
    - keep and extend `check` so it syntax-checks all new Phase 2 modules/scripts
    - add `check:runtimes` for runtime config validation
    - add script entries for later smoke scripts: `smoke:scout`, `smoke:sentinel`, `smoke:tts`, `smoke:pty`, and `smoke:phase2`

    The later scripts may point to files that are created by later plans. This keeps `package.json` edits in Plan 02-01 and avoids same-wave conflicts.
  </action>
  <verify>
    Run `npm.cmd install`.
    Run `npm.cmd run check`.
  </verify>
  <acceptance_criteria>
    - `node-pty` and `chokidar` are present in package metadata.
    - Existing `npm start` still exists.
    - `npm.cmd run check` passes after the plan is complete.
  </acceptance_criteria>
</task>

<task id="T2" type="auto">
  <title>Create runtime definitions</title>
  <files>
    <file>src/runtimes.js</file>
    <file>scripts/check-runtimes.js</file>
  </files>
  <action>
    Create `src/runtimes.js` exporting `RUNTIMES`, `getRuntime(runtimeId)`, and `listRuntimes()`. Define:
    - `claude`: label `CLAUDE CODE`, command `claude`, args `["--dangerously-skip-permissions"]`, rulesFile `CLAUDE.md`, skillsDir `.claude/skills/`, models `["opus-4", "sonnet-4", "haiku"]`, purple badge metadata.
    - `codex`: label `CODEX`, command `codex`, args `["--approval-mode", "full-auto"]`, rulesFile `AGENTS.md`, skillsDir `.codex/skills/`, models `["gpt-4o", "gpt-4.1", "o3", "o4-mini"]`, green badge metadata.

    Create `scripts/check-runtimes.js` that imports the module and asserts required fields exist for both runtimes. Do not spawn `claude` or `codex`.
  </action>
  <verify>
    Run `node --check src/runtimes.js`.
    Run `node --check scripts/check-runtimes.js`.
    Run `npm.cmd run check:runtimes`.
  </verify>
  <acceptance_criteria>
    - Runtime definitions match the PRD.
    - Runtime validation script passes.
    - No runtime process is spawned.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:

```powershell
npm.cmd run check
npm.cmd run check:runtimes
```
</verification>

<success_criteria>
- Dependencies needed by Phase 2 are installed.
- Runtime contract exists and validates.
- Requirements RUN-01, RUN-02, RUN-06, and RUN-07 are represented as configuration, not real process launch.
</success_criteria>
